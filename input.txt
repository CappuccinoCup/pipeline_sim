main:
    mov r0, #6
    bl  fact
    b   exit
fact:
    sub sp, sp, #8
    str lr, [sp, #4]
    str r0, [sp, #0]
    cmp r0, #1
    bne l1
    mov r0, #1
    add sp, sp, #8
    mov pc, lr
l1:
    sub r0, r0, #1
    bl  fact
    mov r12, r0
    ldr r0, [sp, #0]
    ldr lr, [sp, #4]
    add sp, sp, #8
    mov r1, r0
    mul r0, r1, r12
    mov pc, lr
exit:
    Exit
